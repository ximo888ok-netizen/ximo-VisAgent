# 2026-09-02

## Desktop AGI（D:\ximo\ximo-AGI）前后端深度审查
- 产出报告：D:\ximo\ximo-AGI\desktop-agi-code-review.md（P0×5 / P1×14 / P2×15，含修复优先级序列）
- 审查中主窗口前端（App/main/pages/store，约800行）被外部进程实时删除且仓库无 .git 无法恢复；electron.vite.config 此前就只配了 island 入口，主窗口 UI 本已不可构建。需先 git init 再谈修复。
- 关键结论：安全层三处静默失效（用户规则未注入 SafetyClassifier、classify 从不传 domain 致银行黑名单永不命中、open_app 走 cmd.exe start 存在命令注入且为 L1 免审批）；API Key 明文存储无 safeStorage；截图 4K PNG 未压缩直发视觉模型（vision.ts/MindiffChangeDetector 是死代码且 downsample 实现本身把压缩字节当 RGBA 像素，是坏的）；审批超时 checkTimeout 无人调用、waitApproval 无限轮询；"未完成"会被 parseModelOutput 协议3 误判为完成；element_scroll 不移动鼠标；browser ref→元素映射断裂；sidecar 无 DPI manifest 存在坐标虚拟化风险；busyWait 同步忙等可冻结主进程（含急停热键派发）。
- 亮点：岛 IPC zod 双向契约规范、koffi SendInput 字节布局正确、文件沙箱路径防护正确、单文件行数纪律 100% 达标、生产链路零 mock。
- 修复顺序已写入报告 §6：git init → open_app 白名单 → 规则接线 → 截图压缩 → safeStorage → 主窗口去留决策 → loop 健壮性 → DPI/忙等。
