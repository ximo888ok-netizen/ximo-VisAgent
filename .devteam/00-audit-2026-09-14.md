# 项目深度审视报告（现状审计 + 可强化方向）

> 阶段 0（审计）。由项目总管汇总 4 路深度审阅（引擎核心 / 主进程与 Mission / 前端与原生 / 工程健康度）。
> 基线：2026-09-14 工作区（含 108 个未提交文件，+2401/−999）。
> 关联上游文档：`mission-knowledge-development-plan.md`、`docs/fix-plan-agent-reliability.md`、`docs/engineering.md`。

## 一、健康度快照

- `pnpm lint / budget / test / typecheck` 全绿（26 测试文件）；行数门禁名义合规（budget.json 仅豁免 win32.ts）。
- 修复计划 8 项中 **6 项已实施**（条目 2/4/5/6/7/8），条目 1/3 主体完成但有尾巴。
- Mission 计划仅完成 **M1 数据底座（半成）+ M2 粗排（半成）**，编排核心 M3~M7 未动。

## 二、风险清单（按严重度）

| # | 风险 | 证据 | 等级 |
|---|---|---|---|
| R1 | **能力库 IPC 直写绕过宪法门且不审计**，违反计划不变量 | `main/ipc/mission-handlers.ts:44-64`；meta-gate 白名单无 `capability_update`（`meta-gate.ts:37-58`） | 高（安全） |
| R2 | **loop.ts 有效行 394/400，仅剩 6 行余量**，下个改动必爆门禁 | 实测；方案 B（拆审批块 loop.ts:309-381）未执行 | 高（工程） |
| R3 | **108 文件多主题混合大 diff 未提交**（UI 令牌化 + 内核止损 + 通知），难拆分回滚 | git status；lint 6/7 缓存命中，新代码校验新鲜度有限 | 高（流程） |
| R4 | botToken 明文落盘，chmod 0600 在 Windows 基本无效 | `wechat-bot.ts:427-441` | 中高（安全） |
| R5 | 微信审批"回复同意"文案无入站解析承接，指示落空 | `wechat-notify.ts:30` vs `wechat-bot.ts:302-312` | 中 |
| R6 | uia-client 重启预算不复位：累计 5 次后 sidecar 永久失联且无降级广播 | `control-kit/src/uia-client.ts:96-105,146-148` | 中 |
| R7 | 混合 DPI 多屏坐标必错（全局单对 scaleX/scaleY） | `screen-scale.ts:5-11` | 中 |
| R8 | recovery / autoresume / intent / state-tracker 零单测（条目 6 已上线却静默失效风险） | 各 `__tests__` 无引用 | 中 |
| R9 | DB 迁移无版本戳（无 user_version/版本表），append-only 补列无法探测漂移 | `audit-db/migrations.ts:52-68` ≡ `mission-db/migrations.ts:90-96` | 中 |
| R10 | aura 窗与岛共用 preload，可调 islandAPI 全集（含 emergencyStop/审批），仅口头约定隔离 | `main/windows/aura.ts:7-9` | 中低 |

## 三、可强化方向（按 ROI 排序）

### A. 收尾 Mission——让库"活"起来（价值锚点）
1. **mission-runner 接线**：子任务 → `startTask()` 派发 + 终态回写（计划 §4.4）。当前 mission-db 是空转的，queued 任务永不执行。
2. **capability 写入收口**：新增 `capability_update` 过宪法门 + 审计（修 R1）。
3. **补齐契约字段**：subtasks `dependsOn/risk/attempts/task_id`、missions `plan_json`、artifacts `content_hash/stale`、状态枚举加 planning/awaiting_confirm → 才谈得上 DAG 与计划确认闸。
4. FTS5 改 trigram + selftest 补 mission 建表/检索/越权写用例（收 M1/M2 DoD）。
5. **KnowledgePanel + 计划确认卡**前端（计划已列验收项，现为空置）。

### B. 可靠性闭环（引擎 + 通道）
6. 补 recovery/autoresume 单测；planFirst:true 集成回归；UIA→SoM→grounding→目测降级链级集成测试。
7. 微信审批入站解析（同意/拒绝 → decideApproval），让条目 1 真正双向；botToken 改用 safeStorage/DPAPI。
8. uia-client 成功即复位重启计数 + 健康事件上报编排器做降级熔断；中文输入"注入后回读验证 + 重试"。

### C. 工程结构还债（防患未然）
9. **先拆 loop.ts 审批块（方案 B）**，恢复行数余量，再动任何引擎需求。
10. 临界巨石预警（原始行）：wechat-bot 471、executor 462、mission-repo 407——趁 budget 纪律尚在主动拆。
11. 迁移加版本戳机制；**立即把 108 文件 diff 按主题拆 3 个 commit 落盘**（UI 令牌化 / 内核机制 / 通知链路）。
12. 多屏 per-monitor DPI 映射；压缩触发改 token 感知（现为固定 40 步阈值）。

## 四、建议推进顺序

第一批（1-2 天）：#11 拆 commit → #9 loop.ts 腾挪 → #2 宪法门收口（三项都是"先止血"）。
第二批：A 组 Mission 主线（1→3→4→5）。
第三批：B 组可靠性闭环 + C 组余下项。
